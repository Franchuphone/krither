"use client";

import CustomButton from "./CustomButton";

// Link back to the landing page.
const HomeButton = () => {
  return <CustomButton path="/" text="Accueil" variant="outline" />;
};

export default HomeButton;
